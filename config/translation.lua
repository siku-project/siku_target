TranslationConfig = {
  --- Language
  ---
  --- The language used for every target translation: the Lua strings
  --- (keybind label, console) and the interface, whose strings are compiled
  --- from web/messages/<language>.json and pushed again at startup.
  ---
  --- Available: 'en', 'fr'
  ---
  --- Default: 'fr'
  language = 'fr',
}
